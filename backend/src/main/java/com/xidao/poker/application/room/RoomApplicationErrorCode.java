package com.xidao.poker.application.room;

public enum RoomApplicationErrorCode {
    ROOM_ALREADY_EXISTS,
    ROOM_NOT_FOUND,
    MEMBER_ALREADY_CONNECTED,
    MEMBER_NOT_CONNECTED,
    STALE_CONNECTION,
    ROOM_NOT_EMPTY,
    ROOM_CLOSED
}
