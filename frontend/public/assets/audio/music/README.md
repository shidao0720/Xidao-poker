# Public music

`xidao-signal-loop.wav` is an original deterministic ambient loop generated by
`scripts/generate-public-bgm.mjs`. It contains no sampled or third-party audio.

Regenerate it from the repository root with:

```shell
node scripts/generate-public-bgm.mjs
```

The generated file is distributed under the repository MIT license.
